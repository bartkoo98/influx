spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa

  liquibase:
    contexts: dev
    change-log: classpath:database/liquibase-changelog.xml
  jpa:
      hibernate:
        ddl-auto: validate